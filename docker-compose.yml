services:
  # Service for up docker in dev mode with live refresh
  #frontend-avvillas-dev:
  #  image: frontend-avvillas-dev-img
  #  build:
  #    context: .
  #    dockerfile: Dockerfile.dev
  #  volumes:
  #    - .:/app
  #    - /app/node_modules
  #  ports:
  #    - "5173:5173"
  #  container_name: frontend-avvillas-dev
  #  environment:
  #    - CHOKIDAR_USEPOLLING=true

  # Service for up docker in prod mode with NGINX
  frontend-avvillas:
    image: frontend-avvillas-img
    build: .
    volumes:
      - .:/app
    ports:
      - "3000:80"
    container_name: frontend-avvillas
